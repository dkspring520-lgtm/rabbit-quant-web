"use client";

import {useMemo,useState} from "react";
import "./alpha-lab-panel.css";

export type AlphaLabItem={id:string;name:string;expression:string;score:number;status:"promote"|"observe"|"reject"|"insufficient";metrics:{trades:number;ic:number;winRate:number;netPct:number;stability:number;positiveFoldRatio:number}};
const statusLabel={promote:"进入候选池",observe:"继续观察",reject:"淘汰",insufficient:"样本不足"};
export default function AlphaLabPanel({items=[]}:{items?:AlphaLabItem[]}){const[filter,setFilter]=useState<"all"|AlphaLabItem["status"]>("all");const filtered=useMemo(()=>filter==="all"?items:items.filter(item=>item.status===filter),[items,filter]);const counts=useMemo(()=>({tested:items.length,promoted:items.filter(item=>item.status==="promote").length,observing:items.filter(item=>item.status==="observe").length,rejected:items.filter(item=>item.status==="reject").length}),[items]);return <section className="alpha-lab-panel"><header><div><p>🐰 兔兔 Alpha 实验室</p><h2>自动发现、验证和淘汰做T因子</h2><span>只展示滚动样本外结果，不把训练集成绩直接用于实盘。</span></div><strong>{counts.promoted}<small>通过因子</small></strong></header><div className="alpha-lab-stats"><div><b>{counts.tested}</b><span>本轮测试</span></div><div><b>{counts.promoted}</b><span>进入候选池</span></div><div><b>{counts.observing}</b><span>继续观察</span></div><div><b>{counts.rejected}</b><span>自动淘汰</span></div></div><nav>{(["all","promote","observe","reject","insufficient"] as const).map(value=><button type="button" className={filter===value?"active":""} onClick={()=>setFilter(value)} key={value}>{value==="all"?"全部":statusLabel[value]}</button>)}</nav><div className="alpha-lab-list">{filtered.length?filtered.map(item=><article key={item.id} data-status={item.status}><div><h3>{item.name}</h3><code>{item.expression}</code></div><strong>{item.score.toFixed(1)}<small>综合分</small></strong><dl><div><dt>样本外胜率</dt><dd>{(item.metrics.winRate*100).toFixed(1)}%</dd></div><div><dt>IC</dt><dd>{item.metrics.ic.toFixed(3)}</dd></div><div><dt>交易数</dt><dd>{item.metrics.trades}</dd></div><div><dt>稳定度</dt><dd>{(item.metrics.stability*100).toFixed(0)}%</dd></div><div><dt>正收益折数</dt><dd>{(item.metrics.positiveFoldRatio*100).toFixed(0)}%</dd></div><div><dt>净收益</dt><dd>{item.metrics.netPct.toFixed(2)}%</dd></div></dl><span className="alpha-status">{statusLabel[item.status]}</span></article>):<div className="alpha-empty">暂无符合当前筛选条件的因子</div>}</div></section>}
